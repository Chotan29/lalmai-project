<?php require __DIR__.'/vendor/autoload.php'; $app = require_once __DIR__.'/bootstrap/app.php'; $kernel = $app->make(Illuminate\Contracts\Http\Kernel::class); $kernel->handle(Illuminate\Http\Request::capture()); use App\Models\Faculty; try { $faculty = Faculty::first(); echo 'FACULTY_ID=' . ($faculty ? $faculty->id : 'NULL'); } catch (\Exception $e) { echo 'ERROR: ' . $e->getMessage(); } 
